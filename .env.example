# ORE AI-RAG - environment template (optional).
# Copy to `.env` (gitignored) and edit as needed.

# Ollama
ORERAG_OLLAMA_HOST=http://127.0.0.1:11434

# Embedding model (Ollama)
ORERAG_EMBED_MODEL=nomic-embed-text

# Chunking defaults (chars)
ORERAG_CHUNK_SIZE=1000
ORERAG_CHUNK_OVERLAP=200
